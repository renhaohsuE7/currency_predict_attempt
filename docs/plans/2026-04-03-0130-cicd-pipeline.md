# Plan #0130: CI/CD Pipeline (GitHub Actions)

- **Date**: 2026-04-03
- **Status**: completed
- **Module**: .github/workflows/, pyproject.toml
- **Priority**: P2

## 目標

建立 GitHub Actions CI pipeline，自動化 lint、type check、test、coverage report。

## 實作摘要

### Workflows

#### `.github/workflows/ci.yml` — 主 CI

- **觸發**：push/PR to `main` / `transfomer_attempt`
- **Jobs**：
  1. **lint** — `black --check src/ tests/` + `flake8 src/`
  2. **typecheck** — `mypy src/currency_predictor/`
  3. **test** — `pytest --cov` (fast tests only) + upload coverage.xml

#### `.github/workflows/slow-tests.yml` — 慢速測試

- **觸發**：`workflow_dispatch` (手動) + 每週一 06:00 UTC
- **Jobs**：
  1. `pytest -m slow` (offline E2E + pretrained)
  2. `pytest -m e2e` (requires network, continue-on-error)

### 其他變更

- `pyproject.toml` `[dependency-groups] dev` 新增 `black`, `flake8`, `mypy`
- `README.md` 新增 CI status badge

## 完成標準

- [x] Push to main/PR 觸發 CI (workflow file created)
- [x] lint (black + flake8) step
- [x] typecheck (mypy) step
- [x] 292 fast tests step with coverage
- [x] README 有 CI status badge
- [ ] CI 實際執行通過（需 push 到 GitHub 驗證）
