# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime data + training/eval outputs — regenerated by yfinance collection,
# pipeline runs, and model training; should not be version-controlled.
data/raw/
data/processed/
results/
test_models/
*.ckpt
lightning_logs/

# Offline producer output (scripts/generate_forecast.py, generate_eval.py,
# fetch_fundamentals.py). Transient hand-off files imported into the parent
# app's DB; regenerable.
forecast_*.json
fx_eval_*.json
fundamentals*.csv
daily_ohlcv*.csv
universe_*.csv
